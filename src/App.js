import React from "react";
import "./App.scss";
import { Login, Register } from "./components/login/index";


import { BrowserRouter, Switch } from 'reacr-router-dom'

class IndexComponent extends React.Component {

    render() {
        return (
            <BrowserRouter>
            <switch>
                <Route exact path={} />
            </switch>
            </BrowserRouter>
        );

    }
    
};

export default App;